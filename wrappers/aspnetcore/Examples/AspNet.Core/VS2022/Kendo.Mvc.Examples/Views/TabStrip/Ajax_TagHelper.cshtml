<div class="wrapper">
    <kendo-tabstrip name="ajax-tabstrip">
        <items>
            <tabstrip-item text="Dimensions & Weights" content-url="@Url.Content("~/shared/web/tabstrip/ajax/ajaxContent1.html")" selected="true">
            </tabstrip-item>
            <tabstrip-item text="Engine" content-url="@Url.Content("~/shared/web/tabstrip/ajax/ajaxContent2.html")">
            </tabstrip-item>
            <tabstrip-item text="Chassis" content-url="@Url.Content("~/shared/web/tabstrip/ajax/ajaxContent3.html")">
            </tabstrip-item>
        </items>
    </kendo-tabstrip>
</div>


<style>
    .wrapper {
        height: 455px;
        margin: 20px auto;
        padding: 20px 0 0 0;
        background: url('@Url.Content("~/shared/web/tabstrip/bmw.png")') no-repeat center 60px transparent;
    }
    #tabstrip {
        max-width: 400px;
        float: right;
        margin-bottom: 20px;
    }
    .specification {
        max-width: 670px;
        margin: 10px 0;
        padding: 0;
    }
    .specification dt, dd {
        max-width: 140px;
        float: left;
        margin: 0;
        padding: 5px 0 8px 0;
    }
    .specification dt {
        clear: left;
        width: 100px;
        margin-right: 7px;
        padding-right: 0;
        opacity: 0.7;
    }
        .specification:after, .wrapper:after {
            content: ".";
            display: block;
            clear: both;
            height: 0;
            visibility: hidden;
        }
</style>
